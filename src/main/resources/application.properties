# --- Configurações do Banco de Dados (Já existentes) ---
spring.application.name=oasis-api
spring.datasource.url=jdbc:h2:mem:oasisdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true

# --- OpenAI (Mantenha sua chave aqui) ---
spring.ai.openai.api-key=${OPENAI_API_KEY}
spring.ai.openai.chat.options.model=gpt-3.5-turbo

# --- MENSAGERIA ---
# (Se não estiver usando Docker agora, pode comentar para evitar erros de conexão)
# spring.rabbitmq.host=localhost

# --- INTERNACIONALIZAÇÃO ---
spring.messages.basename=messages
spring.messages.encoding=UTF-8

# ==========================================
# 🔐 CREDENCIAIS DE ACESSO (FIXAS)
# ==========================================
spring.security.user.name=admin
spring.security.user.password=123456